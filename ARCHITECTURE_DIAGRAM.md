# Architecture Diagram: EspoCRM + Vercel Integration

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         INTERNET                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │
            ┌─────────────────┴─────────────────┐
            │                                   │
            ▼                                   ▼
  ┌──────────────────┐              ┌──────────────────┐
  │  Vercel Website  │              │     Customer     │
  │                  │              │     Browser      │
  │ wildrosepainters │              │                  │
  │      .ca         │              │  Submits Lead    │
  └─────────┬────────┘              └────────┬─────────┘
            │                                │
            │ API Call (HTTPS)               │
            │ POST /api/leads                │
            │                                │
            └────────────────┬───────────────┘
                             │
                             ▼
            ┌────────────────────────────────┐
            │    Cloudflare Tunnel           │
            │    (Optional but Recommended)  │
            │                                │
            │  Maps public domain to local   │
            │  crm.wildrosepainters.ca       │
            │         ↓                      │
            │    localhost:8080              │
            └────────────────┬───────────────┘
                             │
                             ▼
  ┌──────────────────────────────────────────────────┐
  │              YOUR LAPTOP                          │
  │                                                   │
  │  ┌────────────────────────────────────────────┐  │
  │  │         Docker Containers                  │  │
  │  │                                            │  │
  │  │  ┌──────────────┐    ┌─────────────────┐ │  │
  │  │  │   EspoCRM    │◄───┤   MySQL 8.0     │ │  │
  │  │  │              │    │   Database      │ │  │
  │  │  │  Port: 8080  │    │                 │ │  │
  │  │  │              │    │  Stores Leads,  │ │  │
  │  │  │ - Lead Mgmt  │    │  Contacts,      │ │  │
  │  │  │ - API        │    │  Accounts       │ │  │
  │  │  │ - Workflows  │    │                 │ │  │
  │  │  │ - Email      │    └─────────────────┘ │  │
  │  │  └──────┬───────┘                        │  │
  │  │         │                                 │  │
  │  │         │ Sends Email Notifications       │  │
  │  │         ▼                                 │  │
  │  │  ┌──────────────┐                        │  │
  │  │  │  SMTP Server │                        │  │
  │  │  │  (Gmail/etc) │                        │  │
  │  │  └──────────────┘                        │  │
  │  └────────────────────────────────────────────┘  │
  └──────────────────────────────────────────────────┘
                             │
                             │ Email Notification
                             ▼
                  ┌──────────────────┐
                  │   Your Email     │
                  │                  │
                  │  "New Lead from  │
                  │   Website!"      │
                  └──────────────────┘
```

## Data Flow: Customer Submits Lead

```
1. Customer visits website
   https://wildrosepainters.ca
   
2. Fills out contact form
   ├─ Name
   ├─ Email
   ├─ Phone
   ├─ Service Interest
   └─ Message
   
3. Form submits to Vercel API route
   POST /api/leads
   
4. Vercel API calls EspoCRM
   ├─ URL: https://crm.wildrosepainters.ca/api/v1/Lead
   ├─ Headers: X-Api-Key: YOUR_KEY
   └─ Body: Lead data (JSON)
   
5. EspoCRM receives lead
   ├─ Validates data
   ├─ Creates Lead record
   ├─ Stores in MySQL database
   └─ Triggers workflow
   
6. Workflow sends email notification
   ├─ Uses SMTP configuration
   ├─ Sends to configured email
   └─ Includes lead details
   
7. You receive notification
   └─ Can login to CRM to manage lead
```

## Deployment Options Comparison

### Option A: Local Testing Only
```
[Browser] → http://localhost:8080 → [EspoCRM]
     
✓ Simple setup
✗ Only accessible on laptop
✗ Cannot integrate with Vercel
✗ No HTTPS
```

### Option B: Cloudflare Tunnel (Recommended)
```
[Internet] → https://crm.wildrosepainters.ca
              ↓
         [Cloudflare Edge]
              ↓
      [Cloudflare Tunnel Client]
              ↓
      [Laptop: localhost:8080]
              ↓
          [EspoCRM]

✓ No port forwarding needed
✓ Automatic HTTPS
✓ Works behind any firewall
✓ Free tier available
✗ Requires tunnel running
```

### Option C: Port Forwarding + Caddy
```
[Internet] → https://crm.wildrosepainters.ca
              ↓
    [Your Router (Port 443)]
              ↓
    [Laptop: Caddy (Port 443)]
              ↓
    [Caddy → localhost:8080]
              ↓
          [EspoCRM]

✓ Full control
✓ Automatic SSL (Caddy)
✗ Requires port forwarding
✗ Needs static/dynamic DNS
✗ Firewall configuration
```

### Option D: VPS Deployment
```
[Internet] → https://crm.wildrosepainters.ca
              ↓
    [DigitalOcean/Linode Droplet]
              ↓
         [Caddy (Port 443)]
              ↓
    [EspoCRM Container (Port 8080)]
              ↓
    [MySQL Container]

✓ Always online
✓ Professional setup
✓ Better performance
✓ Automatic SSL
✗ $5-6/month cost
```

## Security Layers

```
┌──────────────────────────────────────────────┐
│  Layer 1: HTTPS/TLS Encryption              │
│  All traffic encrypted in transit            │
└────────────────┬─────────────────────────────┘
                 │
┌────────────────▼─────────────────────────────┐
│  Layer 2: API Key Authentication            │
│  X-Api-Key header required for API access    │
└────────────────┬─────────────────────────────┘
                 │
┌────────────────▼─────────────────────────────┐
│  Layer 3: EspoCRM Permissions                │
│  Role-based access control (RBAC)            │
└────────────────┬─────────────────────────────┘
                 │
┌────────────────▼─────────────────────────────┐
│  Layer 4: Database Isolation                 │
│  MySQL with separate credentials             │
└────────────────┬─────────────────────────────┘
                 │
┌────────────────▼─────────────────────────────┐
│  Layer 5: Docker Container Isolation         │
│  Separate network for containers             │
└──────────────────────────────────────────────┘
```

## Environment Variables Flow

```
┌─────────────────────────────────────────┐
│         Vercel Dashboard                │
│                                         │
│  Settings → Environment Variables       │
│  ├─ ESPOCRM_BASE_URL                   │
│  └─ ESPOCRM_API_KEY                    │
└──────────────┬──────────────────────────┘
               │
               │ Available in runtime
               ▼
┌──────────────────────────────────────────┐
│    Vercel API Route (/api/leads)         │
│                                          │
│  const baseUrl = process.env.            │
│    ESPOCRM_BASE_URL                      │
│  const apiKey = process.env.             │
│    ESPOCRM_API_KEY                       │
└──────────────┬───────────────────────────┘
               │
               │ Makes API call
               ▼
┌──────────────────────────────────────────┐
│    EspoCRM API                           │
│    https://crm.wildrosepainters.ca       │
│    /api/v1/Lead                          │
│                                          │
│    Headers: X-Api-Key: [API_KEY]         │
└──────────────────────────────────────────┘
```

## Workflow: Lead Assignment

```
┌──────────────────────────────────────────────┐
│         New Lead Created                     │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────┐
│    EspoCRM Workflow Engine                   │
│    Trigger: After Record Created             │
└────────────────┬─────────────────────────────┘
                 │
        ┌────────┴────────┐
        │                 │
        ▼                 ▼
┌─────────────┐   ┌──────────────────┐
│ Send Email  │   │ Assign to User   │
│ Notification│   │ (Round Robin)    │
└─────────────┘   └──────────────────┘
```

## Backup Strategy

```
┌──────────────────────────────────────────┐
│    Automated Backup Script               │
│    (Windows Task Scheduler)              │
└────────────────┬─────────────────────────┘
                 │ Daily at 2 AM
                 ▼
┌──────────────────────────────────────────┐
│    Docker Exec → MySQL Dump              │
│    docker exec espocrm-db mysqldump...   │
└────────────────┬─────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────┐
│    Save to Local + Cloud                 │
│    ├─ C:\Backups\espocrm-YYYY-MM-DD.sql │
│    └─ Upload to OneDrive/Dropbox         │
└──────────────────────────────────────────┘
```

## Performance Optimization

```
┌────────────────────────────────────────┐
│  Vercel Edge (CDN)                     │
│  - Static assets cached globally       │
│  - API routes run on Edge Functions    │
└────────────────┬───────────────────────┘
                 │
                 │ Only API calls to CRM
                 ▼
┌────────────────────────────────────────┐
│  EspoCRM                               │
│  - Indexed database queries            │
│  - Cached entity metadata              │
│  - Optimized for < 100ms response      │
└────────────────────────────────────────┘
```

## Network Topology

### With Cloudflare Tunnel
```
[User Device]
     ↓ HTTPS
[Cloudflare Edge Network]
     ↓ Encrypted Tunnel
[Your Laptop - Tunnel Client]
     ↓ Local
[Docker Bridge Network]
     ├─ [EspoCRM Container]
     └─ [MySQL Container]
```

### With Port Forwarding
```
[User Device]
     ↓ HTTPS
[Your Router]
     ↓ Port 443 → 8443
[Your Laptop - Caddy]
     ↓ Reverse Proxy
[Docker Bridge Network]
     ├─ [EspoCRM Container]
     └─ [MySQL Container]
```

## Cost Comparison Visual

```
Traditional SaaS Stack          Self-Hosted EspoCRM
━━━━━━━━━━━━━━━━━━━━━          ━━━━━━━━━━━━━━━━━━━

CRM Software: $50/mo            CRM: $0 (self-hosted)
Email Service: $20/mo           Email: $0 (SMTP)
Storage: $10/mo                 Storage: Unlimited
Per-User: $15/mo                Users: Unlimited

Total: $95/mo                   Total: $0/mo
Annual: $1,140                  Annual: $0

                     SAVE $1,140/YEAR!

With VPS (Production):
Total: $6/mo
Annual: $72

                     SAVE $1,068/YEAR!
```

## Monitoring Dashboard Layout

```
┌─────────────────────────────────────────────────┐
│          EspoCRM Dashboard                      │
├─────────────────────────────────────────────────┤
│                                                 │
│  📊 Lead Statistics          📧 Recent Leads   │
│  ├─ New: 12                  ├─ John Smith     │
│  ├─ Contacted: 8             ├─ Jane Doe       │
│  ├─ Qualified: 5             └─ Bob Johnson    │
│  └─ Won: 3                                      │
│                              🔔 Tasks           │
│  💰 Revenue Pipeline         ├─ Follow up: J.S │
│  └─ $45,000                  └─ Send quote: JD │
│                                                 │
│  📈 Conversion Rate: 37%     📅 Calendar        │
│                              └─ Meeting: 2pm    │
└─────────────────────────────────────────────────┘
```

This architecture provides a complete, professional CRM solution that scales with your business!
